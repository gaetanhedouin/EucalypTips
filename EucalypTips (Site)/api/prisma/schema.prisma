generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder schema for future site-specific data (roles, trainer workflow, subscriptions, premium content).
model Placeholder {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
}
