# Site API Skeleton

This folder is a clean starting point for the future `Site en ligne` backend.

Initial scope:
- dedicated auth/accounts for site (separate from EucAnalypTips app)
- trainer roles/moderation
- subscriptions/premium content

Current endpoints:
- `GET /health`
